[uwsgi]
base = /home/bitshares/stats.bitshares.eu-backend/
app = app
module = %(app)
callable = app

home = %(base)/env
pythonpath = %(base)

master = true
processes = 5

socket = /tmp/stats.sock
chmod-socket = 666
vacuum = true

die-on-term = true
